# 简历工作室

## 入口与使用

打开本地工作台的“模板与工作室”，选一套模板。也可直接访问 http://127.0.0.1:4174/workspace/#/studio/classic 。

1. 点击“快速生成”，填写姓名、求职方向、联系方式、学校、专业、简介和技能。系统按真实输入生成初稿，不编造经历。
2. 左侧补充教育、实习、项目、技能和奖项，也可点击右侧纸面现有文字直接编辑；纸面编辑会同步左侧内容。
3. 展开“排版与模块”，调整主题色、字号、行距、段间距，隐藏模块或上下移动排序。隐藏不会删除内容。
4. 使用顶部撤销/重做；从模板下拉框切换版式，保留内容。十一套版式均支持相同字段。
5. 本地草稿自动暂存在当前浏览器标签页会话（按登录账号和入口模板区分），刷新可继续；关闭会话可能丢失，需导出 JSON 或点击“存入我的简历”。
6. 登录后保存到个人数据库，可继续修改、查看版本或使用已有 DeepSeek 段落润色流程。AI需先配置个人或服务端密钥；不会自动调用或自动采纳。
7. PDF通过浏览器打印的“另存为 PDF”导出。右侧是连续A4宽度编辑画布，最终分页以打印预览为准。HTML含内联样式、无外部字体依赖；JSON可再次导入继续编辑。

## 模板来源

| 模板 | 来源 | 授权 |
| --- | --- | --- |
| 经典蓝、极简黑白、技术清晰版 | Offerbiu 原有模板 | 项目原创 |
| Even · 留白叙事 | https://github.com/rbardini/jsonresume-theme-even | MIT，Rafael Bardini |
| Class · 蓝调名片 | https://github.com/jsonresume/jsonresume-theme-class | MIT，James Spencer / JSON Resume Contributors |
| Classy · 经典刊物 | https://github.com/JaredCubilla/jsonresume-theme-classy | MIT，Jared Cubilla |

开源模板采用中文A4适配版，不声称与上游输出逐像素一致。原始LICENSE、样式/模板快照、来源树SHA及文件哈希保存在 assets/resume-templates/。模板画廊带有原作和许可链接，示例文案仅用于排版展示，不写入用户新草稿。

## 沙箱与保存方式

- 预览运行于带 sandbox="allow-same-origin" 的 iframe，没有 allow-scripts。预览文档的 CSP 禁止脚本、网络连接、表单提交与外部资源，仅允许内嵌 data 图片和内联本地样式。
- 编辑事件由父页面绑定，结构化字段始终按文本转义，不执行用户HTML/CSS/JS。粘贴仅保留纯文本。
- 服务端继续校验模板ID、个人账号归属、字段长度及版本号；design字段只接受允许的颜色、字号、行距、顺序与隐藏项。
- 不接受任意脚本模板上传，也不提供服务器代码执行。这是简历排版编辑沙箱。
- 账号简历仅点击保存才写数据库；本轮开发没有创建示例账号简历或调用DeepSeek。

## 交付边界

已修改前后端代码及静态资源，保留原账号、简历、投递数据。按用户要求未编译、未运行应用测试、未进行本地浏览器交互或PDF分页验收。

## 新增精选模板

从用户指定的 [ResumeCollection](https://github.com/mmmlllnnn/ResumeCollection) 选择 004、005、018、022、023，共五套；画廊现在共十一套模板。新款名称分别为珊瑚红、海军蓝、石墨灰、宝石绿、雾蓝。原版Word与图片、来源树SHA和仓库MIT声明保存在 `assets/resume-templates/collection/`。网页适配版保留主要版式，并简化不能随内容伸缩的装饰，不等同于原始Word文件的逐像素输出。

## 上传头像与作品图

- 进入任意简历的“头像 / 作品图”模块，选择JPG、PNG、WebP。单张原图不超过8 MB；也可点击纸面头像区域进入该模块。
- 头像可更换、移除、隐藏；可选择跟随模板、圆形、圆角或方形，调整完整显示/填满相框及上下左右取景位置。
- 作品或证书配图最多3张，可添加说明、上下移动、删除。作品模块可在“排版与模块”中排序或隐藏，头像通过独立开关控制。双栏模板的联系方式与技能固定在侧栏，其他模块在主栏内按顺序排列。
- 浏览器重新编码为压缩JPEG，透明背景转白色，原始EXIF不作为简历字段保存。头像最多180 KB，配图每张最多300 KB。
- 图片保存于简历JSON内，跟随账号归属、版本记录及投递快照；不创建公开图片URL。需要点击“保存简历”或“存入我的简历”才持久写入数据库。
- 导出的JSON可还原图片与显示设置，HTML为内嵌图片，PDF打印包含当前可见图片。AI润色只发送选中的文字与岗位要求，不发送头像或作品图。
- 后端限制图片数量、数据URL类型、文件头和大小；仅接受JPEG/PNG/WebP，不接受SVG、远程图片地址或脚本。

本轮按要求没有编译、应用测试、浏览器交互验收或真实用户图片上传测试，启动与文件记录不等于导出分页验证。
